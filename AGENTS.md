# 営業管理システム 開発エージェント共通ガイド

担当AIは作業前に、次を順に行う。

1. `docs/project-memory/01_SYSTEM_DESIGN.md`から`04_CURRENT_STATUS.md`までを読む。
2. `docs/ROADMAP.md`を読み、完了済み・進行中・今後予定を確認する。
3. ブランチ、最新コミット、リモートとの差、未コミット・未追跡ファイルを確認する。
4. `docs/verification/README.md`から今回必要な検証手順だけを読む。

## 正式な情報源

- 設計思想・ロードマップ・開発方針：既存設計書
- 現在の開発判断に必要な要約：Project Memory
- 実装：プロジェクト内のコードと設定
- 確認方法：`docs/verification/`
- 変更履歴と現在差分：Git

Project Memoryは既存設計書を置き換えない。資料、コード、Git履歴が矛盾する場合は、実態を調査して矛盾を報告し、推測で正式仕様を作らない。

## 開発思想

- 北極星は「地域情報をもとに予定を宣言し、実行し、記録し、振り返る」状態である。
- 営業担当を監視するのではなく、営業担当と管理者の認識をそろえる。
- フォームやLINE WORKSは入口、Google Sheetsは管理・記録、ダッシュボードは判断・振り返りに使う。
- 使われない高機能より、毎日使える簡単な機能を優先する。
- `地域情報ID → PlanID → 訪問履歴ID → 紹介実績`の追跡線を壊さない。

## 作業原則

- 現在地を整理する前に実装を始めない。
- 1ステップにつき1つの目的だけを扱う。
- 既存変更を上書き、破棄、巻き戻ししない。
- 変更前に対象、影響範囲、変更しない範囲を明示する。
- 未確定事項を担当AIだけで確定しない。
- 設計変更が必要な場合は、理由、メリット、デメリット、影響範囲を説明して確認を受ける。
- 問題発生時は、設計変更より先に最新ベースラインとの差分を調べる。
- 既存フォームは原則再構築せず、構造変更時はバックアップと切替計画を先に作る。
- 安定稼働中の既存モジュールを別機能の都合で直接改修せず、新機能は専用モジュールから疎結合で連携する。
- Apps Scriptエディタでの直接編集は行わず、リポジトリを正本として変更する。

## 責務分割

- `SalesPlan.js`：営業予定フォーム・営業予定シートの作成と設定
- `SalesPlanEngine.js`：営業予定の展開、PlanID、状態管理
- `RegionInfo.js`：地域情報フォーム・シートの作成と設定
- `RegionInfoEngine.js`：地域情報の転記、営業予定変換、重複防止
- `VisitReport.js`：営業報告、訪問履歴、営業予定との接続
- `Dashboard.js`：閲覧用ダッシュボード
- `SystemSetup.js`：作成・更新などのセットアップ
- `Diagnostics.js`：状態調査と診断
- LINE WORKS連携：設計確定後に専用モジュールを追加し、既存エンジンとは入力境界で接続する。

## 検証

- JavaScript変更後は対象ファイルへ`node --check`を実行する。
- `git diff --check`、`git diff --stat`、`git diff`で差分を確認する。
- Apps Script反映前に対象、影響、復元方法、許可を確認する。
- 反映後は対象関数、ログ、シート、フォームまたはLINE WORKS上の実動作を確認する。
- 確認用データは事前値を記録し、確認後に復元する。
- 実機確認が必要な変更は、確認完了前に安定地点として保存しない。

## GitHub保存ルール

- 必要な検証と実機確認を終えた安定状態だけを保存する。
- 機能単位・Step単位で保存し、無関係な変更を混ぜない。
- commit・push前に`docs/verification/release-checklist.md`を使う。
- commit・pushは利用者の許可後に行う。
- push後にローカルと`origin`の同期、作業ツリーの状態を確認する。

## チャット運用

- 開始時にPhase、Step、ベースライン、Git状態、今回の1目的を報告する。
- コードを出す前に、対象と変更しない範囲を短く明示する。
- 実施できない確認だけを、開く場所、操作、正常条件、返してほしい根拠とともに依頼する。
- 終了時に、変更内容、非変更範囲、確認結果、未確認事項、次の1作業を報告する。
